import 'package:flutter/material.dart';

class ScanContactScreen extends StatelessWidget {
  const ScanContactScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}